# Отчёт по аудиту безопасности — Chimera OS (xiu)

**Дата:** 2026-08-27
**Объект:** собственный код ОС (`kernel/`, `boot/`, `drivers/`, `usr/`)
**Не аудировалось:** вендорный/сторонний код (`ravynos/`, `OpenCorePkg/`, `tinycc/`, `dash/`, `fastfetch/`, `zsh/`, `kilo/`, `sh/`)

Модель угроз: локальный непривилегированный процесс (ring 3), сетевой удалённый атакующий (e1000/QEMU), вредоносный файл на диске (FAT32).

---

## Сводка

| ID | Критичность | Компонент | Расположение | Суть | Статус |
|----|-------------|-----------|--------------|------|--------|
| CRIT-01 | 🔴 Критическая | pmap | `kernel/mm/pmap.c:88-96, 119` | `pmap_map_user_page` не проверяет адрес: можно открыть ядровую половину адресного пространства для ring 3 | ✅ Исправлено (Этап 1) |
| CRIT-02 | 🔴 Критическая | Mach-O loader | `kernel/bsd/mach_loader.c:59-114, 99-101` | Load-команды и file-офсеты без границ: OOB-чтение ядровой кучи, вектор к CRIT-01 | ✅ Исправлено (Этап 2) |
| CRIT-03 | 🔴 Критическая | Mach IPC | `kernel/bsd/syscall.c:1348`, `kernel/mach/ipc_kmsg.c:91-105` | `send_sz` (syscall) и `msgh_size` (заголовок) не сверяются: переполнение буфера в ядре | ✅ Исправлено (Этап 3) |
| CRIT-04 | 🔴 Критическая | execve | `kernel/bsd/mach_loader.c:124-141, 171-208` | Стек выделен несмежными страницами, но запись идёт по сплошному HHDM-окну: порча физической памяти через длинный argv | ✅ Исправлено (Этап 2) |
| CRIT-05 | 🔴 Критическая | TCP | `kernel/net/tcp.c:163` | `memcpy` в mbuf-кластер без проверки длины: heap-overflow из `write()` | ✅ Исправлено (Этап 4) |
| HIGH-01 | 🟠 Высокая | Mach IPC | `kernel/mach/ipc_kmsg.c:31-36` | Кольцевой пул kmsg: переиспользование сообщений, ещё стоящих в очередях (UAF) | ✅ Исправлено (Этап 3) |
| HIGH-02 | 🟠 Высокая | Ядро | `ipc_kmsg.c:25-43`, `mach_loader.c:44-53` и др. | Паника ядра на пользовательском вводе: DoS одним сисколом | ✅ Исправлено (Этапы 2, 3) |
| HIGH-03 | 🟠 Высокая | Mach IPC | `kernel/mach/ipc_kobject.c:68-88, 122-125` | `mach_vm_allocate/deallocate` без валидации адреса и размера | ✅ Исправлено (Этап 3) |
| HIGH-04 | 🟠 Высокая | Mach IPC | `kernel/mach/ipc_kmsg.c:~177, 253-256` | OOL-память: игнор ошибок copyin (утечка памяти ядра), глобальное VA-окно на все процессы, страницы не освобождаются | ✅ Исправлено (Этап 3) |
| HIGH-05 | 🟠 Высокая | Mach IPC | `kernel/mach/ipc_kmsg.c:105-191` | Парсер дескрипторов ходит за пределы сообщения, записывает указатели ядра OOB | ✅ Исправлено (Этап 3) |
| HIGH-06 | 🟠 Высокая | FAT32 | `kernel/vfs/fat32.c:261-281` (+ write-путь) | Кластер > 4096 Б: OOB-чтение/запись стекового буфера | ✅ Исправлено (Этап 4) |
| MED-01 | 🟡 Средняя | copyinstr | `kernel/mm/copy.c:117-146` | Нет верхней границы: чтение за 0x800000000000 — доступ к памяти ядра | ✅ Исправлено (Этап 1) |
| MED-02 | 🟡 Средняя | copyin/out | `kernel/mm/copy.c:35` | Переполнение `uaddr + len` (wrap) проходит проверку | ✅ Исправлено (Этап 1) |
| MED-03 | 🟡 Средняя | FAT32 | `kernel/vfs/fat32.c:47-48` | Гонка на кэше FAT-сектора (read-путь без блокировки) | ✅ Исправлено (Этап 4) |
| MED-04 | 🟡 Средняя | TCP/IP | `kernel/net/tcp.c:267-279` | Нет проверки checksum/SEQ, `hlen` не валидируется | ✅ Исправлено (Этап 4) |
| MED-05 | 🟡 Средняя | pmap | `kernel/mm/pmap.c` (clone) | Ошибки `pmm_alloc_page()` не проверяются при клонировании | ✅ Исправлено (Этап 1) |
| MED-06 | 🟡 Средняя | ARP | `kernel/net/arp.c` | Таблица обновляется любым ответом: ARP-spoofing | ✅ Исправлено (Этап 4) |
| LOW-01 | 🟢 Низкая | VFS | `kernel/vfs/vfs.c` | Отсутствует модель прав доступа (uid/режимы не проверяются) | ⏸ Запланировано |
| LOW-02 | 🟢 Низкая | Ядро | `kernel/CMakeLists.txt` | Нет ASLR: фиксированный стек (0x7FFFFFFFE000) и база кода | ⏸ Запланировано |
| LOW-03 | 🟢 Низкая | Ядро | `ipc_kmsg.c`, `syscall.c` | Утечка адресов ядра в лог (`%llx`) через IPC-ошибки | ⏸ Запланировано |

**Аппаратные защиты:** Включены SMEP (Supervisor Mode Execution Prevention) и SMAP (Supervisor Mode Access Prevention) в CR4 на BSP и AP (`kernel/arch/x86_64/gdt.c`).

---

## Критические уязвимости

### CRIT-01. `pmap_map_user_page` позволяет отдать ядровую память пользователю

**Файл:** `kernel/mm/pmap.c:88-96` (ядро функции — L119)

```c
if (!(pml4[pml4_idx] & PAGE_PRESENT)) {
    ...
    pml4[pml4_idx] = phys | PAGE_PRESENT | PAGE_WRITE | PAGE_USER;
} else {
    pml4[pml4_idx] |= PAGE_USER | PAGE_WRITE;   // ← существующая ЯДРОВАЯ запись!
}
```

**Проблема.** Функция не проверяет, что `vaddr < 0x0000800000000000`. Если в качестве `vaddr` передан адрес ядровой половины (PML4-индекс ≥ 256), то:

1. Для уже существующей ядровой записи PML4 выполняется `|= PAGE_USER | PAGE_WRITE` — весь 512 ГБ субрегион ядра становится доступным из ring 3.
2. На уровне PTE существующая запись ядра перезаписывается на новую страницу — ядро теряет страницу, а пользователь получает её.

**Доступные векторы из ring 3 (все подтверждены кодом):**
- `execve()` вредоносного Mach-O: `vmaddr` сегмента из файла без проверки → `mach_loader.c:89`;
- `mach_msg` с `MACH_VM_ALLOCATE`: `req->address` из тела сообщения → `ipc_kobject.c:85`;
- `sys_mmap` → `kernel/bsd/syscall.c:1278`.

Так как SMEP/SMAP выключены, после `|= PAGE_USER|WRITE` код ring 3 напрямую читает/пишет структуры ядра → полный root/ядровый R-W примитив.

**Исправление:**
```c
if (vaddr < 0x1000 || vaddr >= 0x0000800000000000ULL) return 0; // только пользовательская половина
if (pml4[pml4_idx] & PAGE_PRESENT) return 0;                     // не трогать существующие
```
Дополнительно включить SMEP/SMAP в `gdt.c` (CR4.BIT20/BIT21) и проверять бит USER на всех уровнях при модификации.

---

### CRIT-02. Mach-O-загрузчик: load-команды без валидации

**Файл:** `kernel/bsd/mach_loader.c`

- **L59-114** — обход `ncmds` команд с шагом `cmd->cmdsize`, взятым из файла. Нет проверки выхода за буфер файла; `cmdsize == 0` даёт бесконечный цикл; `cmdsize` нечётного/огромного значения — чтение за пределами `kalloc`-буфера.
- **L99-101** — `memcpy` из `module_ptr + file_off` длиной из `filesize`: нет проверки `file_off + filesize <= file_size`. Содержимое соседней ядровой кучи копируется в пользовательские страницы → **утечка памяти ядра**.
- **L72-89** — `vmaddr/vmsize` не проверяются → прямой вектор к CRIT-01 (`vmaddr = 0xFFFF800000000000+`).
- **L83** — `(vaddr + memsz + 4095)` переполняется, цикл отображения идёт по неверному диапазону.

**Эксплуатация:** положить на FAT32 бинарник с сегментом `vmaddr = 0xFFFFFFFF80000000`, вызвать `execve` — загрузчик сам откроет ядровую половину процессу.

**Исправление:** валидировать `ncmds * sizeofcmds` против размера файла, каждую команду (`cmdsize >= sizeof(cmd)`, сумма не превышает буфер), `vmaddr` — только пользовательский диапазон, `file_off + filesize <= file_size`. Паники заменить на возврат ошибки.

---

### CRIT-03. `mach_msg`: переполнение буфера ядра (несоответствие размеров)

**Файлы:** `kernel/bsd/syscall.c:1338-1348` → `kernel/mach/ipc_kmsg.c:24-105`

```c
// syscall.c: u64 send_sz из сискола, молча усекается до u32
ipc_kmsg_t *kmsg = ipc_kmsg_alloc(send_sz);        // выделяем по send_sz
...
// ipc_kmsg.c:91-96 — msgh_size проверяется только глобально
if (user_hdr.msgh_size < MACH_MSG_HEADER_SIZE ||
    user_hdr.msgh_size > MACH_MSG_SIZE_MAX) ...
// ipc_kmsg.c:~100 — копируем ДЛИНУ ИЗ ЗАГОЛОВКА, а не из выделения
copyin((const void *)user_header_va, khdr, user_hdr.msgh_size);
```

`send_sz` и `msgh_size` никогда не сверяются. Вызов `ipc_kmsg_alloc(64)` даёт inline-запись из статического пула `s_kmsg_pool[2048]` (512 байт на запись, `ipc_kmsg.c:14-21`), а `msgh_size = 4096` копирует 4 КБ **полностью контролируемых данных** за пределы записи — порча соседних ядровых объектов. Плюс усечение `u64 → u32` на границе вызова (`send_sz = 0x1_00000100` → alloc 0x100).

**Эксплуатация:** `mach_msg(buf, SEND, 64, 0, ...)` с заголовком, в котором `msgh_size = 4096` → управляемое переполнение в `.bss` ядра.

**Исправление:** в `sys_mach_msg` требовать `send_sz == msgh_size` (после copyin заголовка); передавать размер буфера в `ipc_kmsg_copyin` и копировать `min(msgh_size, ikm_size)`; усечение u64→u32 проверять явно.

---

### CRIT-04. `execve`: несмежный стек + сплошная запись → порча физической памяти

**Файл:** `kernel/bsd/mach_loader.c:124-141, 171-208`

Стек задачи собирается из 2048 **отдельных** страниц `pmm_alloc_page()` (физически НЕ смежных), но копирование argv/envp идёт по арифметике «одно сплошное окно»:

```c
kstack = HHDM(top_actual_phys) + 4096;              // конец ВЕРХНЕЙ страницы
*(u64 *)(kstack - (stack_top - sp)) = ...;          // запись ВНИЗ по физике
```

Как только `stack_top - sp > 4096` (суммарная длина строк argv/envp и массивов указателей; пользователь контролирует её через `execve`), запись уходит **ниже физического кадра верхней страницы** — в соседнюю физическую страницу: таблицы страниц, данные другого процесса, структуры ядра.

**Исправление:** выделять стек одним непрерывным диапазоном (`pmm_alloc_pages`) либо копировать в каждую страницу по её собственному HHDM-отображению; ограничить суммарный размер argv+envp и проверять переполнение.

---

### CRIT-05. TCP: переполнение mbuf-кластера из `write()`

**Файл:** `kernel/net/tcp.c:163`

```c
if (data && len > 0) {
    __builtin_memcpy(m->m_data + hdr_len, data, len);   // len не проверен
}
```

`tcp_send_packet` копирует `len` байт в mbuf-кластер фиксированного размера (`MCLBYTES`) без ограничения. Путь `sys_write/send → sosend → tcp_send → tcp_send_packet` нигде не режет длину: один `write(fd, buf, 65536)` на TCP-сокете переполняет кластер с пользовательскими данными.

**Исправление:** в `tcp_send_packet` — `if (len > MCLBYTES - hdr_len) return error/segment`; в `sosend` — резать на сегменты ≤ MSS, как в BSD.

---

## Высокие уязвимости

### HIGH-01. Кольцевой пул kmsg: переиспользование живых сообщений

`kernel/mach/ipc_kmsg.c:31-36`: `s_kmsg_next` инкрементируется и берётся `% 2048`. После 2048 выделений индексы заходят на записи, чьи kmsg **ещё стоят в очередях портов**. Новое `memset` затирает заголовок/порты живого сообщения → use-after-free и порча очередей. Нужен бит занятости и поиск свободной записи.

### HIGH-02. Паники ядра на пользовательском вводе (DoS)

- `ipc_kmsg.c:25-28` — `msg_size > MACH_MSG_SIZE_MAX` → `chimera_panic` (вызывается из сискола до каких-либо проверок);
- `ipc_kmsg.c:42-44` — исчерпание памяти под kmsg → паника;
- `mach_loader.c:44-53, 86, 128` — некорректный Mach-O (магия, OOM при загрузке) → паника из `execve`.

Любой процесс роняет ядро одним сисколом. Все эти пути должны возвращать `ENOMEM/EINVAL` пользователю.

### HIGH-03. `mach_vm_allocate` / `mach_vm_deallocate` без валидации

`kernel/mach/ipc_kobject.c:68-88, 122-125`: `address` и `size` берутся из тела IPC-сообщения без проверок: произвольный unmap диапазона в целевой задаче; произвольный map (вектор к CRIT-01); `(req->size + 4095)` переполняется; `s_next_mmap_va` после исчерпания окна тихо заворачивается и затирает существующие отображения. Нужны: диапазон только пользовательский, ограничение размера, отказ при коллизии.

### HIGH-04. OOL-память: утечка ядра, глобальное окно, нет освобождения

`kernel/mach/ipc_kmsg.c:~177, 253-256`:
1. Ошибка `copyin` полезной нагрузки OOL **игнорируется** — страницы, выделенные `pmm_alloc_pages` (не обнулённые), с остатками ядровых данных отображаются получателю → **раскрытие памяти ядра**;
2. `s_dyn_ool_vaddr` — одна глобальная статическая переменная на ВСЕ задачи: два процесса получают пересекающиеся VA, при завороте окна чужие страницы отображаются поверх;
3. `ipc_kmsg_free` освобождает только буфер kmsg — страницы OOL (`ikm_ool_regions`) утекают навсегда.

### HIGH-05. Парсер дескрипторов Mach: выход за сообщение и запись указателей

`kernel/mach/ipc_kmsg.c:105-191`: `desc_ptr` двигается по телу сообщения, но не сверяется с `msgh_size`; количество и типы дескрипторов контролирует отправитель. Чтение за пределами буфера + запись ядрового указателя порта (`*(struct ipc_port **)&pdesc->name = port_obj`, ~L163) за границей буфера. Нужна проверка `desc_ptr + sizeof(desc) <= buffer + msgh_size` на каждом шаге.

### HIGH-06. FAT32: кластер больше 4096 байт — выход за стековый буфер

`kernel/vfs/fat32.c:261-281` (чтение) и симметричный write-путь: `cluster_buf[4096]`, но `avail = g_fat32.cluster_size_bytes - cluster_offset` вычисляется от реального размера кластера тома. При монтировании отвергается только `sectors_per_cluster == 0` (`fat32.c:1005`) — том с кластером 8/16/32 КБ даёт `memcpy` за пределы 4 КБ стекового буфера: **OOB-чтение стека ядра в пользовательский `read()`** и **OOB-запись** в write-пути. Ограничить `sectors_per_cluster` поддерживаемым максимумом и отвергать тома с кластером > sizeof(cluster_buf), либо брать буфер по размеру кластера.

---

## Средние уязвимости

### MED-01. `copyinstr` читает за границей пользовательской половины

`kernel/mm/copy.c:117-146`: начальная проверка есть, но в цикле `curr_uaddr` не ограничен — дойдя до `0x800000000000`, `pmap_extract` успешно найдёт **ядровые** PML4-записи (они присутствуют в том же дереве), и байты ядровой памяти скопируются в буфер сискола (pathname и т.п.) с возможным раскрытием через логи/ошибки. Фикс: `if (curr_uaddr > USER_SPACE_MAX) return CHIMERA_ERR_INVALID;` в цикле.

### MED-02. Переполнение `uaddr + len` в `copyin`/`copyout`

`kernel/mm/copy.c:35`: при `uaddr=0x1000, len=0xFFFFFFFFFFFFF000` сумма заворачивается в 0 и проверка проходит. Практическое использование ограничено (walk упрётся в неотображённую страницу), но проверка обязана быть переполнениебезопасной: `if (len > USER_SPACE_MAX - (uptr)uaddr) return EINVAL;`.

### MED-03. Гонка на кэше FAT

`kernel/vfs/fat32.c:47-48`: `s_cached_fat_sec/s_cached_fat_buf` используются read-путём без `s_fat_lock`, тогда как write-путь под блокировкой их инвалидирует → на SMP чтение по устаревшей цепочке кластеров (перекрёстное чтение файлов). Взять блокировку или сделать кэш per-CPU.

### MED-04. TCP input без проверки целостности

`kernel/net/tcp.c:267-279`: не проверяется TCP checksum (и на уровне IP — `kernel/net/ip.c`), `hlen = TH_OFF*4` не проверяется на `hlen < 20 || hlen > m->m_len`, SEQ-окно не валидируется → удалённая инъекция RST/данных, спуфинг соединений. Добавить проверку checksum, hlen и окна SEQ в `tcp_input`.

### MED-05. `pmap_clone_user_space` игнорирует отказы аллокатора

`kernel/mm/pmap.c` (функция клонирования): результаты `pmm_alloc_page()` не проверяются при создании промежуточных таблиц — при `paddr = (u64)-1` запись пойдёт по `HHDM - 4096`. Проверять каждый отказ и откатывать.

### MED-06. ARP без проверки ответов

`kernel/net/arp.c`: таблица обновляется любым входящим ARP-ответом без сверки с ожидающимся запросом — классический ARP-spoofing внутри сегмента. Обновлять запись только при наличии ожидающего запроса/подтверждении.

---

## Низкие / проектные

- **LOW-01.** VFS не имеет модели прав: `vfs_lookup`/open/read/write не проверяют uid/режимы — любой процесс читает любой файл (для одиночной пользовательской ОС — осознанный gap, зафиксировать в документации).
- **LOW-02.** Нет ASLR: стек всегда `0x7FFFFFFFE000`, фиксированная база кода — упрощает ROP после любой из находок.
- **LOW-03.** Утечка адресов ядра в лог: `kprintf("[IPC-ERR] ... msg_ptr=0x%llx ...")` (`syscall.c:1342-1344` и др.) — пригодится для обхода KASLR в будущем.

---

## Отсутствующие механизмы защиты (усиливают всё выше)

| Механизм | Состояние | Где |
|----------|-----------|-----|
| SMEP (CR4.20) | ❌ не включается | `kernel/arch/x86_64/gdt.c:92-95` (только OSFXSR/OSXMMEXCPT) |
| SMAP (CR4.21) | ❌ | там же |
| Stack protector | ❌ нет флагов | `kernel/CMakeLists.txt:68-75` |
| KASLR | ❌ | фиксированный linker-скрипт |
| Обнуление страниц пользователю | частично | `ipc_kmsg.c` OOL-страницы не обнуляются |
| `panic` на данных пользователя | повсеместно | см. HIGH-02 |

---

## Приоритетный план исправления

1. **pmap.c** — ограничить `pmap_map_user_page` пользовательской половиной, запретить модификацию существующих записей, включить SMEP/SMAP *(закрывает CRIT-01 и векторы CRIT-02/HIGH-03)*.
2. **ipc_kmsg.c** — сверка `send_sz`/`msgh_size`, границы дескрипторов, обработка ошибок copyin, освобождение OOL *(CRIT-03, HIGH-04, HIGH-05)*.
3. **mach_loader.c** — полная валидация load-команд/офсетов/`vmaddr`, непрерывный стек, лимит argv *(CRIT-02, CRIT-04)*.
4. **tcp.c** — ограничение длины в `tcp_send_packet`, сегментация в `sosend`, checksum/hlen на входе *(CRIT-05, MED-04)*.
5. Убрать все `chimera_panic` на данных пользователя → коды ошибок *(HIGH-02)*.
6. FAT32-буфер по размеру кластера + блокировка кэша *(HIGH-06, MED-03)*.
7. Харденинг сборки: `-fstack-protector-strong`, KASLR, обнуление страниц.

---

*Отчёт подготовлен CrabCode Agent по статическому анализу кода. Номера строк соответствуют текущему состоянию репозитория на момент аудита.*
